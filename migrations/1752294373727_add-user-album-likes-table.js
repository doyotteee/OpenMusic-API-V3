exports.up = (pgm) => {
  // This migration file is not needed as user_album_likes table 
  // is already created in the main migration file
  // No operations needed
};

exports.down = (pgm) => {
  // No operations needed as table is managed in main migration
};
